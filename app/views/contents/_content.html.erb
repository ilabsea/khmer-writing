<tr>
    <td><%= content.id %></td>
    <td><%= content.content %></td>
    <td><%= content.content_in_khmer %></td>
    <td><%= content.clue %></td>
    <td><%= link_to 'Edit', edit_lesson_content_path(@lesson, content) %></td>
    <td><%= link_to 'Destroy', lesson_content_path(@lesson, content),
            method: :delete,
            data: { confirm: 'Are you sure?' } %></td>
  </tr>
